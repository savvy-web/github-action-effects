---
"@savvy-web/github-action-effects": minor
---

## Features

Support file deletions in `GitCommit.createTree` and `commitFiles` via `sha: null` on `TreeEntry` and `FileChange` union types. Fixes #11.
