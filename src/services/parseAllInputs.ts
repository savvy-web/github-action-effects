/**
 * Re-export InputConfig from Action namespace for backwards compatibility.
 * The implementation lives in {@link Action.parseInputs}.
 */
export type { InputConfig, ParsedInputs } from "../Action.js";
